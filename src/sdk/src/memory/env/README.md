# Env

Pure environment-variable resolution for the memory (tinycortex persona) integration.

## Contents

- [`mod.rs`](./mod.rs) — Pure environment-variable resolution for the memory (tinycortex persona) integration.
- [`tests.rs`](./tests.rs) — Tests for the env module.
- [`types.rs`](./types.rs) — Data types for the `env` module.

## Maintenance

Keep this index synchronized when responsibilities move. Put shared data structures in `types.rs`, focused unit tests in `tests.rs` or a sibling `_tests.rs`, and preserve the module-level Rust documentation as the API source of truth.
